SET LINE 150
SET COLSEP '|'
SET VERIFY OFF
COLUMN SQL_TEXT FORMAT A90

SELECT sq.SQL_ID,sq.sql_text FROM DBA_HIST_SQLTEXT sq, DBA_HIST_ACTIVE_SESS_HISTORY se
WHERE sq.SQL_TEXT LIKE '%&SQL_TEXT%'
and se.session_id=&session_id
and se.sql_id=sq.sql_id;