set line 190
set pages 1900
set colseP |
BREAK ON SQL_ID
COLUMN BIND_VALUE FORMAT A40
COLUMN VALUE_STRING FORMAT A55
SELECT DISTINCT SQL_ID,NAME,POSITION,DATATYPE_STRING,VALUE_STRING FROM DBA_HIST_SQLBIND
WHERE SQL_ID LIKE '&sql_id'
ORDER BY POSITION
/

SELECT DISTINCT 'VAR '|| SUBSTR(NAME,2) ||' '|| DATATYPE_STRING ||';' || CHR(10) || ' EXEC '|| NAME|| ':=' || VALUE_STRING || ';' "BIND_VALUE"
FROM
DBA_HIST_SQLBIND
WHERE SQL_ID LIKE '&sql_id'
ORDER BY 1
/